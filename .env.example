# Copy this file to `.env` and fill in real values. Never commit `.env`.
# The container compose file reads BASE_URL, COOKIE_SECRET, and OIDC_* from
# the host environment - `set -a; source .env; set +a` before `compose up`.

#TODO rebuild this with real values (but no secrets lol)